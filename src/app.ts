import { ScheduleInput } from './components/schedule-input';
import { ScheduleList } from './components/schedule-list';

new ScheduleList('todo-schedule');
new ScheduleList('todo-history');
new ScheduleInput();
